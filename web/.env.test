# Committed on purpose (Vite convention: .env.test loads automatically when
# Vitest's mode is "test"; only .env*.local is git-ignored). Fake, harmless
# values — nothing here is a secret, tests never make a real network call to
# any of these hosts (see src/test/mockFetch.ts).
VITE_OIDC_ISSUER=http://localhost:8080/auth/realms/gadonghr
VITE_OIDC_CLIENT_ID=web
VITE_OIDC_REDIRECT_URI=http://localhost:5173/auth/callback
VITE_OIDC_AUDIENCE=gadonghr-services
VITE_SVC_CONFIG_URL=http://localhost:3010
VITE_SVC_I18N_URL=http://localhost:3011
VITE_DEV_BYPASS=false
